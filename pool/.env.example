# Your own compatible Stratum endpoint. This repository never embeds a wallet address.
UPSTREAM_HOST=127.0.0.1
UPSTREAM_PORT=3333
UPSTREAM_TLS=false

# Public miner listener and private local metrics listener.
POOL_PORT=3333
METRICS_PORT=8080
