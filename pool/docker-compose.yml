services:
  stratum-relay:
    image: node:22-alpine
    working_dir: /app
    command: node stratum-relay.js
    restart: unless-stopped
    env_file: .env
    ports:
      - "3333:3333/tcp"
      - "127.0.0.1:8080:8080/tcp"
    volumes:
      - ./:/app:ro
